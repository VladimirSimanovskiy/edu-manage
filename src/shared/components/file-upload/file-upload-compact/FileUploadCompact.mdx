import { Canvas, Meta, Controls, Description } from "@storybook/blocks";
import * as ComponentStories from "./FileUploadCompact.stories";

<Meta of={ComponentStories} />

# FileUploadCompact

`FileUploadCompact` - компактная версия компонента для загрузки файлов с поддержкой drag & drop, валидации размера и типов файлов.

- Поддержка drag & drop и клика для выбора файлов
- Валидация максимального размера файлов и их количества
- Фильтрация по типу файлов (accept)
- Controlled и uncontrolled режимы работы
- Визуальные состояния перетаскивания и ошибок
- Доступность (ARIA-поддержка)

### Базовое использование

<Canvas of={ComponentStories.Default} />

```tsx
import { FileUploadCompact } from "ics-ui-kit/components/file-upload/file-upload-compact";

export const Basic = () => {
	return <FileUploadCompact />;
};
```

### С дефолтными значениями

<Canvas of={ComponentStories.WithDefaultValues} />

### С ограничениями

<Canvas of={ComponentStories.WithConstraints} />

### Controlled режим

<Canvas of={ComponentStories.Controlled} />

### С сообщениями на русском языке

<Canvas of={ComponentStories.WithRussianErrorMessages} />

### С кастомными сообщениями

<Canvas of={ComponentStories.WithCustomErrorMessages} />

### Свойства

<Controls />

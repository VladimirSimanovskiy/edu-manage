import { Meta } from "@storybook/blocks";
import { Canvas, Story } from "@storybook/blocks";
import * as DrawerStories from "./Drawer.stories";

<Meta of={DrawerStories} />

# Drawer

Drawer - это компонент, который отображает содержимое в выдвижной панели снизу экрана.

> Компонент основан на [drawer из shadcn](https://ui.shadcn.com/docs/components/drawer).

[Ссылка на Figma](https://www.figma.com/design/lc2aOtrukFLJQwxxgH3Cgm/UI-kit?node-id=19122-127726).

## API

> 💡 Drawer использует [vaul](https://vaul.emilkowal.ski/getting-started). Доступны все возможности из [API Reference](https://vaul.emilkowal.ski/api).

## Основные возможности

- Отображение содержимого в выдвижной панели
- Настраиваемый заголовок и описание
- Поддержка шаблонов для заголовка и футера
- Различные типы оверлея (default, gradient, transparent)
- Возможность добавления кнопки "Назад"

## Базовое использование

```tsx
import { Drawer, DrawerTitle, DrawerTrigger } from "ics-ui-kit/components/drawer";
import { DrawerContent } from "ics-ui-kit/components/drawer/components/drawer-content/DrawerContent";
import { DrawerBody } from ".ics-ui-kit/components/drawer/components/drawer-body/DrawerBody";
import { DrawerHeader } from "ics-ui-kit/components/drawer/components/drawer-header/DrawerHeader";
import { DrawerFooter } from "ics-ui-kit/components/drawer/components/drawer-footer/DrawerFooter";
import { Button } from "@/components/button";

const BasicDrawer = () => {
	const [open, setOpen] = useState(false);

	return (
		<Drawer open={open} onOpenChange={setOpen}>
			<DrawerTrigger asChild>
				<Button>Open Drawer</Button>
			</DrawerTrigger>
			<DrawerContent>
				<DrawerHeader>
					<DrawerTitle>Заголовок</DrawerTitle>
				</DrawerHeader>
				<DrawerBody>Содержимое компонента</DrawerBody>
				<DrawerFooter>
					<Button
						variant="outline"
						onClick={() => {
							alert("Изменения сохранены");
							setOpen(false);
						}}
					>
						Сохранить
					</Button>
				</DrawerFooter>
			</DrawerContent>
		</Drawer>
	);
};
```

## Примеры

### Базовый Drawer

<Canvas of={DrawerStories.Basic} />

### Drawer с шаблонами

<Canvas of={DrawerStories.TemplateDrawerPlayground} />

## Доступность

Drawer компонент построен на основе Vaul и следует рекомендациям WAI-ARIA для модальных окон. Он включает:

- Правильную фокусировку
- Управление с клавиатуры
- Правильную семантику для скринридеров
- Ловушку фокуса внутри Drawer
